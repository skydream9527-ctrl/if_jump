# 吃了么（If Jump）功能开发与重构计划

## 1. 目标概述
根据项目目录下的多个 `.md` 游戏设计文档，将现有的基础 React+FastAPI 框架升级，完整实现各项关卡机制、UI效果、测试与系统功能。

由于全部 10 章（共 100 关）的独特机制开发工作量巨大，本计划将采用**框架化实现+核心章节优先**的策略，确保基础机制的通用性，并完整交付极具代表性的**第一章（早餐篇）**作为完整开发基准。

## 2. 核心任务拆解

### 第一阶段：核心机制引擎升级（Game Engine 拓展）
当前前端的 `entities.ts` 和 `Game/index.tsx` 仅支持标准（Standard）和移动（Moving）平台。需要拓展支持设计文档中提到的复杂机制：
- **平台形状与渲染拓展**：支持矩形（豆腐）、长条（油条）、圆形（煎饼/包子）、浅口碗等，以及对应的主题渲染。
- **障碍物系统**：实现移动障碍物（如 1-2 油条条），碰撞检测与扣分/弹回机制。
- **动态平台交互**：
  - 倒计时与等待机制（如 1-3 煎饼摊的排队等待，过早/过晚跳跃判定）。
  - 视线遮挡/蒸汽机制（如 1-4 鸡蛋灌饼，定时喷发蒸汽遮挡落点）。
  - 弹性晃动与高精度判定（如 1-6 小笼汤包的汤汁晃动及 ×3 中心高分判定）。
  - 缓慢漂移与摩擦力（滑行）机制（如 1-8 粥铺、1-9 馄饨面）。
- **分支路线支持**：实现岔路机制（如 1-7 豆腐脑的咸甜之争）。

### 第二阶段：数据结构与后端适配
- 重构 `backend/app/seed/levels_data.py` 中的关卡数据。目前 `ALL_LEVELS` 虽然有 100 关，但 `mechanics_config` 为空。
- 为第一章（1-1 至 1-10）填充符合《第一章具体玩法.md》的详细 JSON 配置（包含平台尺寸、障碍物、倒计时、蒸汽等参数）。
- 升级前后端 API，使前端 `generatePlatforms` 能够直接解析后端的 `platforms_config` 和 `mechanics_config`，而不是在前端写死。

### 第三阶段：UI 与特效完善
- 结合《UI详细设计规范.md》与《动画与特效规范.md》：
  - 完善蓄力UI（添加抛物线预测虚线、蓄力阶段颜色变化）。
  - 实现不同类型落点的得分特效（完美、良好、普通）。
  - 添加 UI 气泡对话框（如 NPC 催促）与环境粒子特效（蒸汽、热气）。
  - Boss 关卡特殊 UI 与结算页面的徽章/成就展示。

### 第四阶段：测试与验收
- 编写/运行前端组件测试。
- 模拟跑通第一章的所有关卡，确保碰撞检测、得分逻辑、机制交互与文档要求一致。
- 确认后端 API 的调用记录和结算成绩正确持久化。

## 3. 执行步骤建议
1. **优先修改后端**：完善 `Level` 数据模型和 `levels_data.py`，输出带机制参数的完整第一章数据。
2. **升级前端 Entities**：在 `entities.ts` 引入 `Obstacle`, `Steam`, `WaitTimer` 等新类，并重构 `Platform` 支持更多形态。
3. **改造前端 Game Loop**：在 `index.tsx` 的 update 和 draw 循环中注入新的机制判定。
4. **测试联调**：启动前后端，通过 `OpenPreview` 实际验证游戏手感。

---
*注：由于上下文限制，本次开发将集中在底层引擎重构与第一章的所有核心特色机制落地。后续章节可基于同一套扩展引擎通过配置快速实现。*